// Stub for `server-only` in Vitest unit tests.
// The real package throws to prevent client-component imports;
// in unit tests there is no React server context so we just export nothing.
export {};
